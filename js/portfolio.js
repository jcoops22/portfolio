console.log("portfolio");
const frame = document.querySelector("#frame");

frame.addEventListener("scroll", () => {
  console.log("I have been scrolled on");
  // need to do a scroll to, chec the dpl Project
});
